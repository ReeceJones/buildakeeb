module backend.parts.kbkeycap;

import backend.parts.part;
import backend.structops;

// struct KeyboardKeycap
// {
// 	mixin(inherit!KeyboardPart);
// 	string profile;		/// The profile of the keyset
// 	string[] mountStyles;	/// The mounting styles that the keycap supports. In most cases this should only be one element.
// 	string material;	/// The material that the keycap is made out of.
// }


